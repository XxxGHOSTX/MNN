"""
Matrix Neural Network (MNN) Engine.

A deterministic, non-destructive modular engine for query processing
and sequence analysis. The MNN engine provides a pipeline-based
architecture with strict typing and guaranteed reproducibility.

All components maintain deterministic behavior: identical inputs
always produce identical outputs in the same order.
"""

__version__ = "1.0.0"
